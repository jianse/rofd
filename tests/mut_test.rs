#[cfg(test)]
mod test_mut {
    #[derive(Debug, Default)]
    struct SomeStruct {
        val: u32,
    }

    // #[derive()]
    // struct

    #[test]
    fn test_() {
        let s = SomeStruct::default();
    }
}
